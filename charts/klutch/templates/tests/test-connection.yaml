apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "klutch.fullname" . }}-test-connection"
  labels:
    {{- include "klutch.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "klutch.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
