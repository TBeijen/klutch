apiVersion: v1
kind: Service
metadata:
  name: {{ include "klutch.fullname" . }}
  labels:
    {{- include "klutch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "klutch.selectorLabels" . | nindent 4 }}
